/**
Main class
Annoying issue with output class cursor.
I need to implement my own but i think it will become
relevant during operating system part.
Until then, this project is delayed and time to move to compiler.
*/
class Main {
    function void main() {
        //do StringBuffer.testStringBuffer();
        //do TextRow.testTextRow();
        //do TextBuffer.testTextBuffer(); 
        do TextEditor.programLoop();  
        return;
    }
}
